import styled from 'styled-components';
import { TitleColor } from './color';

//  标题
export const ModuleTitle = styled.h3`
  padding: 10px 10px;
  color: ${TitleColor};
  font-size: 16px;
  font-weight: bold;
  .iconfont {
    font-size: 14px;
    margin-right: 10px;
    color: #89e5ff;
    font-weight: 400;
  }
`;
